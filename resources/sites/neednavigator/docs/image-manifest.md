# image-manifest.md — Need Navigator v2 image slots

Every image slot on the site is listed here. Placeholders are inline SVG / CSS compositions or generated files; each slot's blade file carries a matching `IMAGE SLOT` comment. Work through the "Replace with" column and the build will absorb real assets slot by slot. (Grows as Step 3 pages are built.)

| # | Slot id | Page / location | Current placeholder | Replace with |
|---|---|---|---|---|
| 1 | `logo-header` | Layout header (every page) | `img/nn-logo.png` — the original white wordmark, now on the evergreen masthead. **It is clipped**: the final R touches the right canvas edge at 250x29 with no margin, and it is a raster at 1x. | Vector (SVG) of the **white** wordmark with the life-ring "O" intact, and margin on the right so the R is whole. The recolored dark variant (`nn-logo-dark.png`) is retired and no longer referenced. |
| 2 | `logo-footer` | Layout footer (every page) | `img/nn-logo.png` — legacy white logo, verbatim from the old site | Vector (SVG) white/reversed wordmark for dark backgrounds |
| 3 | `favicon` | Browser tab / bookmarks | `img/favicon.svg` + `favicon-32.png` + `apple-touch-icon.png` — geometric compass-needle mark, evergreen | Brand favicon (ideally derived from the life-ring mark), SVG + 32px PNG + 180px apple-touch |
| 4 | `og-default` | Open Graph / social shares (every page) | `og/og-default.png` — generated: white logo + tagline on evergreen, DejaVu type | Designed 1200×630 OG card using brand type; consider per-page variants for the four solutions pages later |
| 5 | `home-hero` | Home, hero right column | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/hero-jimmy-calvert` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Inline SVG: abstract line illustration — three households connecting into one client record | Warm photograph or commissioned illustration of a caseworker meeting a family across a desk — natural light, unposed, landscape |
| 6 | `home-dashboard` | Home, dashboard feature band | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/dashboard` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS stat tiles + funding-pool meter in a browser frame | Real screenshot of the configurable program dashboard (v2), light theme, landscape — widget board with a query table and funding-pool balance widget |
| 7 | `shelters-bed-matrix` | /features/shelters, top band | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/reservation-floor-plan` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of the bed-availability week matrix | Real screenshot of the bed-availability matrix, light theme, landscape — one week of beds with occupant spans and a maintenance bed visible |
| 8 | `client-records-dossier` | resources/sites/neednavigator/pages/features/client-records.blade.php — 'The one-page dossier' section (section--surface directly below the hero) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/client-dossier` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of the one-page dossier: alert banner, photo/name header marked as a test record, insurance/income/care-team cards, tab row, history rows, and a radial quick-action menu of the eight real actions | Real screenshot of a client profile (one-page dossier), light theme, landscape — ideally a test record, with an active alert banner visible and the radial quick-action menu open |
| 9 | `documents-inbox-triage` | /features/documents — first section after the hero (section--surface), inside .uiframe figure | Stylized HTML/CSS recreation of the Document Inbox triage screen: queue of incoming scans (Scanner/SFTP source tags) on the left, document preview plus Client/Type/Folder/Attach-to filing fields and File/Skip buttons on the right | Real screenshot of the Document Inbox triage screen, light theme, landscape — the queue of incoming scans on the left, one file previewed on the right with its client, document type, and folder fields filled in |
| 10 | `households-panel` | /features/households-care-team — UI representation section (.section--surface directly below the hero) | Stylized HTML recreation of the household panel: four member chips with a head-of-household badge, the member-to-member relationship matrix with one outside-household relationship (grandparent), and the address-change sync prompt with member checkboxes. | Real screenshot of a household record, light theme, landscape — member list with the head-of-household badge visible, the relationship matrix (including at least one outside-household relationship), and the address-change sync prompt offering to copy address/phone/housing status to selected members. Test data only, no real client names. |
| 11 | `income-eligibility-verdict` | /features/income-eligibility — UI representation section (section--surface, directly below the hero) | Stylized HTML recreation of an eligibility check: program limit header (2026, 200% of FPL, household of 4, Marion County), per-member income rows tagged Verified / Self-reported / Not counted (minor income off for the program), verified-annualized-income meter against the limit, and an under-the-limit eligible badge | Real screenshot of a program eligibility check, light theme, landscape — household income compared to a program's income limit, with verified and unverified amounts distinguishable and the eligible verdict visible |
| 12 | `emergency-assistance-voucher` | /features/emergency-assistance — UI-representation section directly below the hero (section--surface) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/need-status-timeline` (from efa2, with the Messages thread; test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of a printable assistance voucher in a .uiframe browser frame: request details (resource, amount, payee), eligibility block (household size, recent annualized income, program maximum, under-limit verdict), and a two-pool funding split reconciled to the request amount | Real screenshot of the printable assistance request/voucher, light theme — request details, the eligibility math (household size, recent income vs. program maximum with the under/over-limit verdict), and a split allocation across two funding pools all visible |
| 13 | `casework-note-thread` | /features/casework — first section below the hero (section--surface), inside a .uiframe figure | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/visit-record` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of a case-note thread: visibility chip (Team · Family Support), read-tracking chip (Read by 2 of 3), an @mention to a supervisor, a manager reply, and an undelivered note showing the undo-window countdown (Delivers in 4:32 · Undo) | Real screenshot of a case note with a reply, light theme — visibility setting, read tracking, an @mention, and the undo-window countdown visible on an undelivered note |
| 14 | `billing-batch` | /features/billing — UI representation section (section--surface, directly below the hero) | Stylized HTML recreation of a billing batch table in a .uiframe browser frame: five records showing minutes-to-units conversion under the 8-minute rule (23→2, 15→1, 40→3, 52→3), one record excluded for insurance coverage dates with a warning flag, carrier stamped per line, and a batched/units/excluded summary footer with an Export to Excel pill | Real screenshot of a generated billing batch, light theme, landscape — minutes-to-units conversion visible on several lines, at least one record excluded for insurance coverage dates, and per client/code/date totals with the carrier stamped |
| 15 | `programs-smart-button-composer` | /features/programs-automation — first section after the hero (section--surface), inside the .uiframe figure | Stylized HTML/CSS recreation of the Smart Button composer: three-step action sequence (visit, need, referral) with pre-set value chips, a note template with template-variable tokens, and dashed 'ask at click' chips | Real screenshot of the Smart Button composer admin screen, light theme, landscape — a button with a three-step action sequence (visit, need, referral), pre-set values, a template variable in a note field, and one prompt-at-click blank visible |
| 16 | `referrals-printed-qr` | resources/sites/neednavigator/pages/features/referrals-partners.blade.php — first section--surface after the hero (the page's single UI representation) | Stylized HTML/CSS recreation of a printed referral inside a .uiframe browser frame: destination organization, contact, reason, referring program, an SVG QR code with 'Scan to record receipt — no login needed', and a loop-closure band showing the received-by note, feedback, and timestamp | Real screenshot (or photo) of an actual printed referral, light theme — destination organization, referral reason, the QR code, and the recorded loop closure (received-by, feedback, timestamp) all visible |
| 17 | `forms-translation-editor` | /features/forms — UI representation section (.section--surface, directly below the hero) | Stylized HTML/CSS recreation of the side-by-side English/Spanish translation editor in a .uiframe browser frame: provenance chips (AI baseline drafted Friday, last hand edit Monday, 2 unsaved changes) above a two-column grid showing a section title, field label, helper text, and answer options in both languages, with a 'hand-edited' chip on the polished Spanish label | Real screenshot of the side-by-side EN/ES translation editor, light theme, landscape — one form section with field label, helper text, and answer options visible in both languages, plus the AI-baseline/hand-edit provenance indicators; no real client data |
| 18 | `intake-submissions-grid` | /features/intake-management — UI representation section (section--surface, directly below the hero) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/submissions-grid + submissions-views` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML recreation of the Quick Forms submissions grid: shared saved-view chip, two presence avatars, five rows with match badges (exact / probable with ranked candidates / new individual), one cell outlined with a teammate's editing tag, one color-tinted row, linked assistance-request statuses (Approved / Ready for review / Pending), and an incremental-loading footer | Real screenshot of the Quick Forms submissions grid, light theme, landscape — cell presence visible on at least one cell, match classifications per row, the status column showing linked assistance-request statuses, and a saved shared view selected |
| 19 | `portal-phone-intake` | Second section (.section--surface) of /features/public-intake-portal, directly after the hero — the page's single UI representation | Stylized HTML/CSS recreation: a phone frame showing guided income capture mid-application (EN/ES language pill, income source card, Add-another-source, Continue) beside a .uiframe browser frame of the intake review queue showing the arrived submission with a 'Probable match' badge and its household income tagged 'Unverified' | Real screenshot pair — the MyNeedNav portal form on a phone (guided income capture step, portrait, light theme) beside the staff review queue showing the arrived submission with a Probable match classification and its income tagged Unverified |
| 20 | `events-public-page` | /features/events — UI representation section, left panel (section--surface, directly under the hero) | Stylized HTML recreation of a published public event page: hero-image block, title, schedule/location, capacity indicator with meter, two ticket types with quantities, an applied promo code chip, and a donation-pledge row with the offline-collection note | Real screenshot of a published public event page, light theme — hero image, schedule, location, capacity indicator, and ticket types visible; demo event data only |
| 21 | `events-dayof-checkin` | /features/events — UI representation section, right panel (section--surface, directly under the hero) | Stylized HTML recreation of the day-of check-in list: checked-in/registered/walk-in counts, a QR check-in with undo, a pending check-in, a walk-in registered at the door, and a no-show | Real screenshot of the day-of check-in screen, light theme — checked-in guests, undo, a walk-in registration, and a no-show visible; test attendees only, no real names |
| 22 | `parent-ed-attendance-grid` | /features/parent-education-classes — UI representation section (section--surface, directly below the hero) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/class-offering` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of the facilitator mobile attendance grid inside a .uiframe browser frame (5 roster rows with P/A/L/E pills, credit-override and walk-in annotations, walk-in button, session note) | Real screenshot of the facilitator mobile attendance grid, light theme, portrait phone crop — present/absent/late/excused marks, a credit override, a walk-in row, and the walk-in button visible |
| 23 | `geotracker-supervisor-map` | /features/geotracker — UI representation section (section--surface, directly after the hero) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/geotracker-map` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized inline-SVG supervisor map inside a .uiframe: filter chips (worker / client / task list / place / dates), clustered markers with counts, two staff-named place labels, one dashed unnamed-place label, and three lone log pins | Real screenshot of the GeoTracker supervisor map, light theme, landscape — clustered markers with counts, at least one staff-named place label, and the filter controls (worker / task list / place / date range) visible in frame |
| 24 | `caa-funding-pools` | /solutions/community-action-agencies — second section (section--surface directly below the hero) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/funding-pools-fy` (composited from funding-pools3, DAP row dropped; test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML recreation of two funding-pool balance meters (reusing .pool-meter) plus a request split across both pools with a reconciliation check, inside a .uiframe browser frame | Real screenshot of the funding pools screen showing balances and date ranges by funder, and a need's split allocation across two pools, light theme, landscape |
| 25 | `shelters-housing-checkout` | /solutions/shelters-housing — UI representation section directly after the hero (section--surface) | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/reservation-floor-plan (also on /features/shelters)` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of a group check-out card: household of three checking out in one action, categorized exit reason (Housing / Moved to transitional housing), bed nights actual vs planned, and the enrollment-closing automation note | Real screenshot of a group reservation check-out for a family — whole-group check-out with exit reason selection and bed nights visible — light theme, landscape |
| 26 | `fbca-distribution-grid` | /solutions/food-banks-community-aid — UI representation section, directly below the hero | Stylized HTML recreation of the Quick Forms submissions grid on a distribution morning: check-in times, household sizes, match badges (exact / probable / new), what each household received, and the linked request's status, with a presence indicator | Real screenshot of the Quick Forms submissions grid worked during a distribution day, light theme, landscape — match badges, household details, and request statuses visible |
| 27 | `parent-ed-solutions-cohort` | /solutions/parent-education — first section below the hero (section--surface), inside .uiframe.ui-cohort | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/class-offering (also on /features/parent-education-classes)` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML recreation of a cohort completion list: per-participant attendance dots against an attend-6-of-8 fidelity threshold, one credit override, certificate-ready and below-threshold chips, and a Generate certificates (PDF) action | Real screenshot of a course offering's completion/roster view, light theme, landscape — fidelity threshold visible, per-participant attendance, at least one credit override and one certificate-ready participant |
| 28 | `reporting-report-builder` | /reporting — second section (section--surface), directly after the page header | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/report-builder` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML recreation of a saved report definition card in a .uiframe browser frame: record type chips (Needs selected, +7 more), column chips, condition-filter chips, a PII-redaction-on row, Same-program visibility, and a Monthly / 3rd Tuesday / 7:00 a.m. / Email + in-app schedule row | Real screenshot of the report builder showing a saved definition — record type, chosen columns, condition filters, PII redaction level, visibility, and a monthly "3rd Tuesday" scheduled delivery all visible; light theme, landscape |
| 29 | `security-flag-queue` | /security — Audit trail & access monitoring section, inside the .uiframe figure | ✅ **Filled 2026-09-04** with a real screenshot: `img/screens/client-history` (test data; sources outside the repo at `/home/ubuntu/sitehub-image-sources/neednavigator/`). Was: Stylized HTML/CSS recreation of the suspicious-access flag queue: two open flags (night-hours access, sequential record walking), two resolved flags with resolution notes, and a CSV-export affordance | Real screenshot of the suspicious-access flag resolution queue, light theme, landscape — rule names visible, at least one open and one resolved flag, no real client or staff names |
| 30 | `about-team` | /about — 'Who we are' section, right column of the hero-grid | Abstract inline-SVG line illustration (site style: evergreen 1.5px strokes on a pale wash circle) of two people at a table working through a shared workflow sheet | Real photograph of the Webriculture team, or of the team working alongside agency staff — candid, natural light, landscape orientation; no stock photography |
